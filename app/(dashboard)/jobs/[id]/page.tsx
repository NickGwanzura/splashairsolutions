import { FeaturePlaceholder } from "@/components/feature-placeholder";

export default function JobDetailPage() {
  return (
    <FeaturePlaceholder
      title="Job Detail"
      description="Job detail screens are still being connected to live data."
      backHref="/jobs"
      backLabel="Back to jobs"
    />
  );
}
